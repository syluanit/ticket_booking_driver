<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.syluanit.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app_logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".Activity.SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.Home" />
        <activity android:name=".Activity.Chon_Dia_Diem" />
        <activity android:name=".Activity.So_Do_Xe_Activity" />
        <!-- <activity -->
        <!-- android:name=".Home" -->
        <!-- android:label="@string/title_activity_home" -->
        <!-- android:theme="@style/AppTheme"></activity> -->
        <activity android:name=".Activity.EditInfoForm" />
        <activity android:name=".Activity.ChangePasswordForm" />
        <activity android:name=".Activity.EditTicketForm" />
        <activity android:name=".Activity.SignIn" />
        <activity android:name=".Activity.Sign_Up_Activity" />
        <activity android:name=".Activity.RouteActivity" />
        <activity android:name=".Activity.PhoneChecking"></activity>
    </application>

</manifest>